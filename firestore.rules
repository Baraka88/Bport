rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * BRJDEV SECURITY RULES (Prototype Mode)
     * 
     * Extremely permissive for prototype development to ensure no permission errors.
     */

    match /{document=**} {
      allow read, write: if true;
    }
  }
}