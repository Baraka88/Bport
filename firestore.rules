
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Community comments are public read, anyone can create
    match /comments/{id} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if false;
    }

    // User profiles: Only owner can manage
    match /chat_users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Chats: Users manage their own conversations
    match /chats/{chatId} {
      allow read: if request.auth != null && resource.data.userId == request.auth.uid;
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
    }

    // Chat Messages: Ownership enforced by direct userId field
    match /chat_messages/{messageId} {
      allow read: if request.auth != null && resource.data.userId == request.auth.uid;
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
    }

    // Professional Inquiries
    match /inquiries_hire_me/{id} {
      allow create: if true;
      allow read: if false;
    }
    match /inquiries_collaboration/{id} {
      allow create: if true;
      allow read: if false;
    }
    match /images/{id} {
      allow read: if true;
    }
  }
}
