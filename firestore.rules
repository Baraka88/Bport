rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Completely open rules as requested for a clean start
    match /{document=**} {
      allow read, write: if true;
    }
  }
}