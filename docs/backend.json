
{
  "entities": {
    "Comment": {
      "title": "Comment",
      "type": "object",
      "properties": {
        "authorName": { "type": "string" },
        "commentText": { "type": "string" },
        "projectId": { "type": "string" },
        "userId": { "type": "string" },
        "parentId": { "type": "string" },
        "likes": { "type": "array", "items": { "type": "string" } },
        "submissionDate": { "type": "string", "format": "date-time" }
      },
      "required": ["authorName", "commentText", "submissionDate"]
    },
    "Project": {
      "title": "Project",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "longDescription": { "type": "string" },
        "tech": { "type": "array", "items": { "type": "string" } },
        "imageUrl": { "type": "string" },
        "liveUrl": { "type": "string" },
        "repoUrl": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "description", "longDescription", "tech", "imageUrl"]
    },
    "InquiryHire": {
      "title": "InquiryHire",
      "type": "object",
      "properties": {
        "clientName": { "type": "string" },
        "clientEmail": { "type": "string", "format": "email" },
        "clientPhone": { "type": "string" },
        "message": { "type": "string" },
        "serviceIds": { "type": "array", "items": { "type": "string" } },
        "budget": { "type": "string" },
        "timeline": { "type": "string" },
        "urgency": { "type": "string" },
        "status": { "type": "string", "enum": ["new", "viewed", "contacted", "closed"] },
        "submissionDate": { "type": "string", "format": "date-time" }
      }
    },
    "InquiryCollab": {
      "title": "InquiryCollab",
      "type": "object",
      "properties": {
        "collaboratorName": { "type": "string" },
        "collaboratorEmail": { "type": "string", "format": "email" },
        "projectIdea": { "type": "string" },
        "estimatedTimeline": { "type": "string" },
        "status": { "type": "string" },
        "submissionDate": { "type": "string", "format": "date-time" }
      }
    },
    "GalleryImage": {
      "title": "GalleryImage",
      "type": "object",
      "properties": {
        "url": { "type": "string" },
        "description": { "type": "string" },
        "altText": { "type": "string" },
        "uploadDate": { "type": "string", "format": "date-time" }
      },
      "required": ["url", "description", "uploadDate"]
    }
  },
  "auth": {
    "providers": ["anonymous", "password"]
  },
  "firestore": {
    "/comments/{commentId}": {
      "schema": "Comment",
      "description": "Community feedback."
    },
    "/projects/{projectId}": {
      "schema": "Project",
      "description": "Dynamic portfolio projects."
    },
    "/inquiries_hire_me/{id}": {
      "schema": "InquiryHire",
      "description": "Inquiries for hire."
    },
    "/inquiries_collaboration/{id}": {
      "schema": "InquiryCollab",
      "description": "Collaboration proposals."
    },
    "/images/{imageId}": {
      "schema": "GalleryImage",
      "description": "Professional gallery images."
    }
  }
}
