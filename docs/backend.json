
{
  "entities": {
    "ChatUser": {
      "title": "ChatUser",
      "type": "object",
      "properties": {
        "username": { "type": "string" },
        "email": { "type": "string" },
        "joinDate": { "type": "string", "format": "date-time" }
      },
      "required": ["username", "email", "joinDate"]
    },
    "Chat": {
      "title": "Chat",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "title": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "title", "createdAt"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "chatId": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "ai"] },
        "text": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "chatId", "role", "text", "createdAt"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/chat_users/{userId}": {
      "schema": "ChatUser",
      "description": "User profiles. Access form must be completed to create this."
    },
    "/chats/{chatId}": {
      "schema": "Chat",
      "description": "User conversation sessions."
    },
    "/chat_messages/{messageId}": {
      "schema": "ChatMessage",
      "description": "Messages within a session. Filtered by chatId."
    }
  }
}
